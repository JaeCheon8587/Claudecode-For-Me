# UI_GUIDE — {프로젝트명} 사용자 인터페이스 가이드

> ⚠ **TEMPLATE** — `{...}` placeholder를 채우거나 해당 줄을 삭제한다. 콘솔/CLI/GUI/Web 등 인터페이스 종류에 맞춰 7항목을 조정한다.
> 작성 규칙은 [DOCUMENT_GUIDE.md](DOCUMENT_GUIDE.md) 참조.

| 항목 | 값 |
|---|---|
| 문서 ID | UI_GUIDE |
| 작성 가정 | {인터페이스 종류 — 콘솔 / GUI / Web 등 + 본 가이드 적용 범위} |
| 관련 문서 | [PRD](PRD.md) · [FRD 폴더](FRD/) · [ADR](ADR.md) |

> 본 문서는 사용자 인터페이스(=출력·입력·플로우 가이드)에 한정한다. 도메인 규칙·API 시그니처는 PRD/FRD 참조.

---

## 1. 개요
{인터페이스의 목적·자동/수동 실행·외부 의존(서비스 URL 등) 명시}

## 2. 출력 원칙
1. {원칙 1 — 예: 흐름 단위로 묶기}
2. {원칙 2 — 예: 사용자 출력과 시스템 로그 분리}
3. {원칙 N}

## 3. 흐름별 출력 포맷
### 3.1 흐름 시작 / 종료
{시작 헤더·종료 푸터 형식}

### 3.2 정상 단계 표시
{정상 단계 출력 예시 (코드 블록)}

### 3.3 의도된 실패 단계 표시
{기대값이 "실패 응답"인 경우의 정상 케이스}

### 3.4 검증 실패 시 출력 (기대값 vs 실제값 diff)
{`[DIFF] field= expected= actual=` 표준 형식 등}

## 4. 표시 기호 규칙
| 기호 | 의미 | 사용 위치 |
|---|---|---|
| `{기호}` | {의미} | {위치} |

## 5. 로그 vs 사용자 출력 구분
| 구분 | 출력 대상 | 형식 |
|---|---|---|
| 사용자 출력 | {stdout / GUI 영역} | {사람이 보는 포맷} |
| 시스템 로그 | {stderr / 별도 sink} | {타임스탬프 + 레벨 + 메시지} |

> 인코딩·언어·접근성 정책 등 부가 사항을 1~2줄로.

## 6. 흐름 자동 실행 순서·중단·계속 규칙
### 6.1 실행 순서
1. {Flow 1}
2. {Flow N}

### 6.2 흐름 간 상태 격리
{각 흐름 시작 직전 상태 리셋 정책}

### 6.3 중단·계속 규칙
| 상황 | 정책 |
|---|---|
| {검증 실패} | {다음 흐름 진행 / 즉시 중단} |
| {통신 실패 N회 누적} | {전체 중단 / 계속} |

### 6.4 종료 요약
{모든 흐름 종료 후 Summary 형식 + 종료 코드}

## 7. 흐름별 출력 예시
### 7.1 {Flow 1 이름}
```
{출력 예시}
```

### 7.2 {Flow 2 이름}
```
{출력 예시}
```
